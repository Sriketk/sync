{
  "name": "@repo/tools",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "stack:up": "docker compose -f ../infra/docker-compose.yml up postgres -d",
    "stack:down": "docker compose -f ../infra/docker-compose.yml down",
    "relay": "bun run --filter relay dev",
    "yjs-ws": "bun run --filter yjs-ws dev"
  }
}
